# apps/backend/.gitignore — only paths unique to this package
# Shared ignores live in the repo-root .gitignore.

# Local machine runtime (scores + edited MachineDef)
/data/runtime/

# SPA build artifacts synced by build:docker (keep .gitkeep)
/public/**
!/public/.gitkeep
