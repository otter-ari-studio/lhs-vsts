# Archived: Web MVP (WS + GrabPart)

**Not part of the product path.** Snapshot of the pre-rewrite Three.js client
that consumed Python WebSocket hand frames and drove GrabPart / LockManager.

Useful as a reference for tips, scoring numbers, and part IDs when building
the new MachineDef + browser tracking stack. Do not import from here in `web/`.
